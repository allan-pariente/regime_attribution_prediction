# ══════════════════════════════════════════════════════════════════════════════
# Financial Regime Attribution Engine — Requirements
# ══════════════════════════════════════════════════════════════════════════════

# ── DATA & CORE ────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scipy>=1.10          # For distance calculations (cosine, euclidean)
python-dotenv>=1.0   # For API key management from .env

# ── FINANCIAL DATA ─────────────────────────────────────────────────────────────
# Source 1 (primary)   : FRED (Federal Reserve Economic Data) — free, no API key
pandas-datareader==0.10.0
# Source 2 (fallback)  : Yahoo Finance
yfinance>=0.2.36
# Source 3 (fallback)  : Alpha Vantage (free API key available)
requests>=2.31

# ── APIs ───────────────────────────────────────────────────────────────────────
# Finnhub (financial news & market data)
finnhub-python>=2.4
# Note: Guardian API uses requests (already listed above)

# ── NLP & EMBEDDINGS ───────────────────────────────────────────────────────────
# FinBERT for financial sentiment analysis
transformers>=4.40
torch>=2.0
# Sentence Transformers for semantic embeddings
sentence-transformers>=2.7

# ── MACHINE LEARNING & EXPLAINABILITY ──────────────────────────────────────────
# Core ML
scikit-learn>=1.4
joblib>=1.3          # For model serialization (save/load .pkl)
# Explainability
shap>=0.45
# Causal inference
statsmodels>=0.14
dowhy>=0.11          # Optional, for causal discovery

# ── VISUALIZATION ──────────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# ── UTILITIES & TOOLS ──────────────────────────────────────────────────────────
tqdm>=4.65           # Progress bars
pyyaml>=6.0          # Config file handling

# ── JUPYTER & NOTEBOOKS ───────────────────────────────────────────────────────
jupyter>=1.0
nbformat>=5.9
ipykernel>=6.0
ipywidgets>=8.0
